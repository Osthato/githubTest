public class MLRunner {
    public static void main(String arg[]) {
		// NeuralNetwork connectXAI = new NeuralNetwork(42, 35, 35, 7);
        // NeuralNetwork connectOAI = new NeuralNetwork(42, 35, 35, 7);
        // ConnectFour game = new ConnectFour();

        // for (int i = 0; i < 100000; i++) {
        //     while (!game.getWon()) {
        //         if (game.getTurn().equals("X")) {
        //             game.takeTurn(connectXAI.interpretResults(connectXAI.predict(game.convertBoard())));
        //         }
        //         else {
        //             game.takeTurn(connectOAI.interpretResults(connectOAI.predict(game.convertBoard())));
        //         }
        //     }
        //     // game.printBoard();
        //     if (game.getTurn().equals("X")) {
        //         connectOAI.replaceWeights(connectXAI);
        //     }
        //     else {
        //         connectXAI.replaceWeights(connectOAI);
        //     }
        //     connectOAI.randomize(0.05);
        //     System.out.print(".");

        // }
        // game.printBoard();
        // System.out.println(connectXAI);
    }
}
